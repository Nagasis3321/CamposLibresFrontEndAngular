import { IdtoDenominacionPipe } from './idto-denominacion.pipe';

describe('IdtoDenominacionPipe', () => {
  it('create an instance', () => {
    const pipe = new IdtoDenominacionPipe();
    expect(pipe).toBeTruthy();
  });
});
