import { FilterAnimalPipe } from './filter-animal.pipe';

describe('FilterAnimalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnimalPipe();
    expect(pipe).toBeTruthy();
  });
});
