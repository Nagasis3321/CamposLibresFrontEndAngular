import { FilterPelajePipe } from './filter-pelaje.pipe';

describe('FilterPelajePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPelajePipe();
    expect(pipe).toBeTruthy();
  });
});
